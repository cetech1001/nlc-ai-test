'use client'

import { PageBackground, Header, TermsOfService } from "@/lib";

const TermsPage = () => {
  return (
    <PageBackground>
      <Header />
      <TermsOfService />
    </PageBackground>
  );
};

export default TermsPage;
