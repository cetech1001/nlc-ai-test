'use client'

import { PageBackground, Header, PrivacyPolicy } from "@/lib";

const PrivacyPage = () => {
  return (
    <PageBackground>
      <Header />
      <PrivacyPolicy />
    </PageBackground>
  );
};

export default PrivacyPage;
