[build]
builder = "dockerfile"
dockerfilePath = "apps/api/users/Dockerfile"
watchPatterns = [
  "apps/api/users/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
