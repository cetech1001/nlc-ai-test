[build]
builder = "dockerfile"
dockerfilePath = "apps/api/users/Dockerfile"
watchPaths = [
  "apps/api/users/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
