[build]
builder = "dockerfile"
dockerfilePath = "apps/api/courses/Dockerfile"
watchPatterns = [
  "apps/api/courses/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
