[build]
builder = "dockerfile"
dockerfilePath = "apps/api/leads/Dockerfile"
watchPatterns = [
  "apps/api/leads/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
