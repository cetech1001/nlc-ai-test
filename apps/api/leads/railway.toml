[build]
builder = "dockerfile"
dockerfilePath = "apps/api/leads/Dockerfile"
watchPatterns = [
  "apps/api/leads/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
