[build]
builder = "dockerfile"
dockerfilePath = "apps/api/analytics/Dockerfile"
watchPatterns = [
  "apps/api/analytics/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
