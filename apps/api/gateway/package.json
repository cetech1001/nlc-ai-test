{
  "name": "@nlc-ai/api-gateway",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prisma:studio": "prisma studio",
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push",
    "prisma:format": "prisma format",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate deploy",
    "prisma:baseline": "prisma migrate resolve --applied 000000000000_init_baseline"
  },
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "node-env=production"
          ]
        },
        "configurations": {
          "development": {
            "args": [
              "node-env=development"
            ]
          }
        }
      },
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@nlc-ai/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@nlc-ai/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@nlc-ai/api-gateway:build:production"
          }
        }
      }
    }
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0"
  }
}
