[build]
builder = "dockerfile"
dockerfilePath = "apps/api/communities/Dockerfile"
watchPatterns = [
  "apps/api/communities/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
