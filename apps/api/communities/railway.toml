[build]
builder = "dockerfile"
dockerfilePath = "apps/api/communities/Dockerfile"
watchPatterns = [
  "apps/api/communities/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
