[build]
builder = "dockerfile"
dockerfilePath = "apps/api/auth/Dockerfile"
watchPatterns = [
  "apps/api/auth/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
