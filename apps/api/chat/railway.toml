[build]
builder = "dockerfile"
dockerfilePath = "apps/api/chat/Dockerfile"
watchPatterns = [
  "apps/api/chat/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
