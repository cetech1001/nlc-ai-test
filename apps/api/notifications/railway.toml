[build]
builder = "dockerfile"
dockerfilePath = "apps/api/notifications/Dockerfile"
watchPatterns = [
  "apps/api/notifications/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
