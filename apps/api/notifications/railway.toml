[build]
builder = "dockerfile"
dockerfilePath = "apps/api/notifications/Dockerfile"
watchPatterns = [
  "apps/api/notifications/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
