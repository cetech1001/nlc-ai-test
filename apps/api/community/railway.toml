[build]
builder = "dockerfile"
dockerfilePath = "apps/api/community/Dockerfile"
watchPatterns = [
  "apps/api/community/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
