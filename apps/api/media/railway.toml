[build]
builder = "dockerfile"
dockerfilePath = "apps/api/media/Dockerfile"
watchPatterns = [
  "apps/api/media/**",
  "libs/api/**",
  "libs/types/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
