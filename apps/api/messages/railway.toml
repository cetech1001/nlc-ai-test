[build]
builder = "dockerfile"
dockerfilePath = "apps/api/messages/Dockerfile"
watchPatterns = [
  "apps/api/messages/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
