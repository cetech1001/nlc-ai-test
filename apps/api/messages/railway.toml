[build]
builder = "dockerfile"
dockerfilePath = "apps/api/messaging/Dockerfile"
watchPatterns = [
  "apps/api/messaging/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
