[build]
builder = "dockerfile"
dockerfilePath = "apps/api/agents/Dockerfile"
watchPatterns = [
  "apps/api/agents/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
