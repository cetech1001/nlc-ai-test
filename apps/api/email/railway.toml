[build]
builder = "dockerfile"
dockerfilePath = "apps/api/email/Dockerfile"
watchPatterns = [
  "apps/api/email/**",
  "libs/api/**",
  "nx.json",
  "package.json",
  "tsconfig*.json"
]

[deploy]
healthcheckPath = ""
restartPolicyType = "never"
