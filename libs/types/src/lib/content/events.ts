/*
import {BaseEvent} from "../base-event";

export interface IntegrationSyncRequestedEvent {
  eventType: 'integration.sync.requested';
  payload: {
    integration: Integration;
    platform: string;
    syncType: 'content' | 'analytics' | 'full';
    requestedAt: string;
  };
}

export interface ContentSyncCompletedEvent extends BaseEvent{
  eventType: 'content.sync.completed';
  payload: {
    integrationID: string;
    userID: string;
    platform: string;
    syncResult: ContentSyncResult;
    completedAt: string;
  };
}*/
